<div class="formOpen animate__animated animate__fadeInDown" style="display: none;">
    <div class="container-fluid my-4 p-5">
        <div class="formOpenLinks">
            {% for link in linklists.abierto.links %}
                {% assign child_list_handle = link.title | handleize %}
                {% if linklists[child_list_handle].links != blank %}
                    <li aria-haspopup="true" class="site-nav--has-dropdown{% if link.active %} site-nav--active{% endif %}">
                        <a href="{{ link.url }}" class="site-nav__link">
                            {{ link.title }}
                            <span aria-hidden="true" class="icon icon-arrow-down"></span>
                        </a>
                        <ul class="site-nav__dropdown">
                            {% for childlink in linklists[child_list_handle].links %}
                                <li {% if childlink.active %} class="site-nav--active" {% endif %}>
                                    <a href="{{ childlink.url }}" class="site-nav__link">{{ childlink.title | escape }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    </li>
                {% else %}
                    <li {% if link.active %} class="site-nav--active" {% endif %}>
                        <a href="{{ link.url }}" class="site-nav__link">{{ link.title }}</a>

                    </li>
                {% endif %}
            {% endfor %}
        </div>
    </div>
</div>

<script>
    $(document).ready(function () {

        $(".header__search").on("click", function () {
            $(".formOpen").toggle();
        })
    })
</script>